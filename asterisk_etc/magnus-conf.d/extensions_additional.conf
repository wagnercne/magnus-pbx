; ============================================
; MAGNUS PBX - Extensions Additional (gerenciado)
; ============================================

#include magnus-conf.d/extensions-features.conf
#include magnus-conf.d/routing.conf
#include magnus-conf.d/tenants.conf

[default]
exten => _X.,1,NoOp(=== Unrouted Call: ${EXTEN} ===)
 same => n,NoOp(Channel: ${CHANNEL})
 same => n,NoOp(Endpoint: ${CHANNEL(endpoint)})
 same => n,Answer()
 same => n,Playback(ss-noservice)
 same => n,Hangup()
