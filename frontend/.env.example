# Magnus PBX Frontend - Environment Variables

# Base URL da API usada no runtime frontend
# Em Docker com nginx proxy, manter /api
VITE_API_BASE_URL=/api

# SignalR Hub URL usada no runtime frontend
# Em Docker com nginx proxy, manter /hubs/asterisk
VITE_SIGNALR_HUB_URL=/hubs/asterisk

# Target do proxy do Vite no desenvolvimento local
VITE_DEV_BACKEND_TARGET=http://localhost:5000

# Asterisk WebRTC Websocket
VITE_ASTERISK_WS_URL=ws://localhost:8089/ws

# Environment (development, staging, production)
VITE_ENV=development

# Enable debug logs
VITE_DEBUG=true
